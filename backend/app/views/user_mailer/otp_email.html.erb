<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title> <strong>E.G.O</strong> | OTP Verification</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap');

        body {
            background-color: #121212;
            color: #E0E0E0;
            font-family: 'Inter', sans-serif;
            margin: 0;
            padding: 0;
            text-align: center;
        }

        .container {
            max-width: 480px;
            margin: 50px auto;
            background: #1E1E1E;
            border-radius: 10px;
            padding: 40px 30px;
            box-shadow: 0px 4px 20px rgba(255, 255, 255, 0.05);
        }

        h1 {
            color: #ffffff;
            font-size: 24px;
            margin-bottom: 10px;
        }

        p {
            font-size: 15px;
            color: #B0B0B0;
            line-height: 1.6;
        }

        .otp {
            background: #ffffff;
            color: #000;
            padding: 12px;
            font-size: 22px;
            font-weight: bold;
            letter-spacing: 5px;
            display: inline-block;
            margin: 20px auto;
            border-radius: 8px;
        }

        .btn {
            display: inline-block;
            padding: 12px 24px;
            background: #0078FF;
            color: #ffffff;
            text-decoration: none;
            font-size: 14px;
            font-weight: 600;
            border-radius: 6px;
            margin-top: 20px;
        }

        .btn:hover {
            background: #005FCC;
        }

        .footer {
            font-size: 12px;
            color: #777;
            margin-top: 30px;
            text-align: center;
        }

    </style>
</head>
<body>
    <div class="container">
        <h1> OTP Verification</h1>
        <p>Hello <strong><%= @user.first_name %></strong>,</p>
        <p>Use the code below to verify your identity:</p>
        <div class="otp"><%= @otp %></div>
        <p>This code is valid for <strong>3 minutes</strong>.</p>
    </div>
    <div class="footer">
        &copy; <%= Time.now.year %> Clutch | Secure & Trusted
    </div>
</body>
</html>
